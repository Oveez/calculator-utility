---
import SiteLayout from '../../layouts/SiteLayout.astro';
import Icon from '../../components/Icon.astro';
import { siteName, toAbsoluteUrl, withBasePath } from '../../utils/site';

const pageTitle = `Research & Statistics Calculators | Sample Size, Z-Score & P-Value · ${siteName}`;
const pageDesc = 'Free online statistics and research calculators. Cochran sample size, margin of error, confidence intervals, Z-score, Student t-score, p-value significance, and A/B test power analysis.';
const canonicalUrl = toAbsoluteUrl('/research-tools/');

const breadcrumbItems = [
  { label: 'Home', href: toAbsoluteUrl('/') },
  { label: 'Research & Statistics' },
];

const statsCalculators = [
  {
    slug: 'sample-size-calculator',
    title: 'Sample Size Calculator',
    badge: 'Research Standard',
    metaDescription: 'Calculate minimum required sample size for surveys, thesis dissertations, and clinical studies using Cochran’s formula and finite population correction.',
    href: withBasePath('/research-tools/sample-size-calculator/'),
    icon: 'math',
  },
  {
    slug: 'margin-of-error-calculator',
    title: 'Margin of Error Calculator',
    badge: 'Survey Quality',
    metaDescription: 'Determine the precision margin (±%) of your survey results based on sample size, confidence level (90%, 95%, 99%), and population size.',
    href: withBasePath('/research-tools/margin-of-error-calculator/'),
    icon: 'math',
  },
  {
    slug: 'confidence-interval-calculator',
    title: 'Confidence Interval Calculator',
    badge: 'Inferential Stats',
    metaDescription: 'Compute 90%, 95%, or 99% confidence interval bounds for sample means (Z or t-distribution) and sample proportions.',
    href: withBasePath('/research-tools/confidence-interval-calculator/'),
    icon: 'math',
  },
  {
    slug: 'z-score-calculator',
    title: 'Z-Score & Percentile Calculator',
    badge: 'Normal Dist',
    metaDescription: 'Convert raw test scores to standard normal Z-scores, calculate percentile rankings, and determine left, right, and two-tailed probabilities.',
    href: withBasePath('/research-tools/z-score-calculator/'),
    icon: 'math',
  },
  {
    slug: 't-score-calculator',
    title: 'T-Score Calculator',
    badge: 'Student-t',
    metaDescription: 'Compute one-sample Student-t test statistics, degrees of freedom, and standardized psychometric T-scores (T = 50 + 10Z).',
    href: withBasePath('/research-tools/t-score-calculator/'),
    icon: 'math',
  },
  {
    slug: 'p-value-calculator',
    title: 'P-Value Calculator',
    badge: 'Hypothesis Test',
    metaDescription: 'Calculate exact one-tailed and two-tailed p-values from Z-scores and t-scores with statistical significance test conclusions (α = 0.05).',
    href: withBasePath('/research-tools/p-value-calculator/'),
    icon: 'math',
  },
  {
    slug: 'standard-error-calculator',
    title: 'Standard Error Calculator (SEM)',
    badge: 'Measurement',
    metaDescription: 'Calculate standard error of the mean (SEM) and standard error of proportions with finite population correction factors.',
    href: withBasePath('/research-tools/standard-error-calculator/'),
    icon: 'math',
  },
  {
    slug: 'ab-test-sample-size-calculator',
    title: 'A/B Test Sample Size Calculator',
    badge: 'CRO & Growth',
    metaDescription: 'Determine traffic and sample size requirements per variant for split tests based on baseline conversion, MDE, statistical power, and test duration.',
    href: withBasePath('/research-tools/ab-test-sample-size-calculator/'),
    icon: 'career',
  },
];

const schemaFaq = [
  {
    question: 'What sample size formula is used for research and surveys?',
    answer: 'Our tool uses Cochran’s formula n₀ = (Z² × p(1 - p)) ÷ e² where Z is the critical value for confidence level, p is expected proportion (50% for maximum variance), and e is the margin of error. When a population size N is provided, Finite Population Correction (FPC) is applied: n = n₀ ÷ (1 + (n₀ - 1) ÷ N).',
  },
  {
    question: 'When should I use a Z-test vs a Student’s t-test?',
    answer: 'Use a Z-test when population standard deviation is known or sample size is large (n ≥ 30) with normal distribution. Use Student’s t-test when population standard deviation is unknown and estimated from sample standard deviation s with degrees of freedom df = n - 1.',
  },
  {
    question: 'How do statistical power and significance level affect A/B test sample size?',
    answer: 'Higher statistical power (e.g., 80% or 90%) reduces the risk of Type II errors (false negatives), while lower significance level α (e.g., 5% or 1%) reduces Type I errors (false positives). Both increase the required sample size per variant.',
  },
];

const schemaGraph = [
  {
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: breadcrumbItems.map((item, index) => ({
      '@type': 'ListItem',
      position: index + 1,
      name: item.label,
      item: item.href,
    })),
  },
  {
    '@context': 'https://schema.org',
    '@type': 'FAQPage',
    mainEntity: schemaFaq.map((item) => ({
      '@type': 'Question',
      name: item.question,
      acceptedAnswer: {
        '@type': 'Answer',
        text: item.answer,
      },
    })),
  },
];
---

<SiteLayout title={pageTitle} description={pageDesc} canonical={canonicalUrl}>
  <script is:inline type="application/ld+json" set:html={JSON.stringify(schemaGraph)} />

  <article class="space-y-10 py-2">
    <!-- Breadcrumbs -->
    <nav aria-label="Breadcrumb">
      <ol class="flex flex-wrap items-center gap-2 text-xs text-[color:var(--muted)]">
        {breadcrumbItems.map((item, index) => (
          <li class="flex items-center gap-2">
            {index > 0 && <span aria-hidden="true">/</span>}
            {item.href ? (
              <a class="transition hover:text-[color:var(--fg)]" href={item.href}>
                {item.label}
              </a>
            ) : (
              <span aria-current="page" class="font-semibold text-[color:var(--fg)]">
                {item.label}
              </span>
            )}
          </li>
        ))}
      </ol>
    </nav>

    <!-- Header Banner -->
    <header class="space-y-4 rounded-3xl border border-[color:var(--border)] bg-[color:var(--surface-strong)] p-6 sm:p-10">
      <div class="inline-flex items-center gap-2 rounded-full border border-[color:var(--border)] bg-[color:var(--surface)] px-3.5 py-1 text-xs font-bold text-[color:var(--fg)]">
        <span>Research Methodology &amp; Inferential Statistics</span>
      </div>
      <h1 class="text-3xl font-extrabold tracking-tight text-[color:var(--fg)] sm:text-4xl">
        Research &amp; Statistics Calculators
      </h1>
      <p class="max-w-3xl text-sm leading-relaxed text-[color:var(--muted)] sm:text-base">
        A rigorous collection of statistical calculation engines for academic researchers, survey designers, data analysts, and CRO growth teams. High-precision calculations for sample sizing, hypothesis testing, confidence intervals, and standard errors.
      </p>
    </header>

    <!-- Grid of 8 Statistics Calculators -->
    <section class="space-y-4">
      <div class="flex items-center justify-between">
        <div>
          <p class="text-xs font-bold uppercase tracking-wider text-[color:var(--muted)]">Statistical Engine</p>
          <h2 class="text-xl font-bold tracking-tight text-[color:var(--fg)]">8 Specialized Research Tools</h2>
        </div>
        <span class="rounded-lg bg-[color:var(--surface)] border border-[color:var(--border)] px-2.5 py-1 text-xs font-bold text-[color:var(--fg)]">
          8 Calculators
        </span>
      </div>

      <div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
        {statsCalculators.map((tool) => (
          <a
            href={tool.href}
            class="group flex flex-col justify-between rounded-2xl border border-[color:var(--border)] bg-[color:var(--surface)] p-5 transition hover:border-[color:var(--border-strong)] hover:bg-[color:var(--surface-strong)]"
          >
            <div class="space-y-2.5">
              <div class="flex items-center justify-between">
                <div class="flex h-9 w-9 items-center justify-center rounded-xl bg-[color:var(--surface-strong)] border border-[color:var(--border)] text-[color:var(--fg)]">
                  <Icon name={tool.icon as any} class="w-4 h-4" />
                </div>
                <span class="rounded-md bg-[color:var(--surface-strong)] border border-[color:var(--border)] px-2 py-0.5 text-[10px] font-bold text-[color:var(--muted)]">
                  {tool.badge}
                </span>
              </div>
              <h3 class="text-base font-bold text-[color:var(--fg)] transition-colors">
                {tool.title}
              </h3>
              <p class="text-xs leading-relaxed text-[color:var(--muted)]">
                {tool.metaDescription}
              </p>
            </div>
            <div class="mt-5 flex items-center gap-1.5 text-xs font-bold text-[color:var(--fg)]">
              <span>Open Calculator</span>
              <Icon name="arrow-right" class="w-3.5 h-3.5 transition group-hover:translate-x-1" />
            </div>
          </a>
        ))}
      </div>
    </section>

    <!-- Value Props -->
    <section class="grid gap-6 sm:grid-cols-3 rounded-2xl border border-[color:var(--border)] bg-[color:var(--surface)] p-6">
      <div class="space-y-1.5">
        <h3 class="font-bold text-sm text-[color:var(--fg)]">Methodological Transparency</h3>
        <p class="text-xs text-[color:var(--muted)] leading-relaxed">
          Every tool exposes its complete underlying statistical formula, critical value lookup, and finite population adjustments.
        </p>
      </div>
      <div class="space-y-1.5">
        <h3 class="font-bold text-sm text-[color:var(--fg)]">Zero Data Logging</h3>
        <p class="text-xs text-[color:var(--muted)] leading-relaxed">
          Proprietary experiment data and clinical research parameters are processed strictly in browser memory without external API calls.
        </p>
      </div>
      <div class="space-y-1.5">
        <h3 class="font-bold text-sm text-[color:var(--fg)]">Academic &amp; Business Utility</h3>
        <p class="text-xs text-[color:var(--muted)] leading-relaxed">
          Equally suited for undergraduate dissertations, peer-reviewed clinical studies, market research polls, and product split tests.
        </p>
      </div>
    </section>

    <!-- FAQs -->
    <section class="space-y-4">
      <h2 class="text-xl font-bold text-[color:var(--fg)]">Frequently Asked Questions</h2>
      <div class="space-y-3">
        {schemaFaq.map((faq) => (
          <details class="group rounded-2xl border border-[color:var(--border)] bg-[color:var(--surface)] p-4 sm:p-5">
            <summary class="cursor-pointer list-none font-semibold text-sm sm:text-base text-[color:var(--fg)] flex items-center justify-between gap-4">
              <span>{faq.question}</span>
              <span class="text-xs text-[color:var(--muted)] group-open:rotate-180 transition-transform">▼</span>
            </summary>
            <p class="mt-3 text-xs sm:text-sm leading-relaxed text-[color:var(--muted)]">
              {faq.answer}
            </p>
          </details>
        ))}
      </div>
    </section>
  </article>
</SiteLayout>
