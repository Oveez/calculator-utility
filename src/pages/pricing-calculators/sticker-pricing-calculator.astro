---
import SiteLayout from '../../layouts/SiteLayout.astro';
import Icon from '../../components/Icon.astro';
import { siteName, toAbsoluteUrl, withBasePath } from '../../utils/site';

const pageTitle = `Sticker Pricing Calculator | Vinyl Sticker & Decal Profit Margins · ${siteName}`;
const pageDesc = 'Calculate custom vinyl sticker production costs per unit, wholesale prices, Etsy/retail margins, and bulk tiered pricing (10, 50, 100, 250 units).';
const canonicalUrl = toAbsoluteUrl('/pricing-calculators/sticker-pricing-calculator/');

const breadcrumbItems = [
  { label: 'Home', href: toAbsoluteUrl('/') },
  { label: 'Pricing Calculators', href: toAbsoluteUrl('/pricing-calculators/') },
  { label: 'Sticker Pricing Calculator' },
];

const faqs = [
  {
    question: 'How do you price handmade or manufacturer-printed stickers?',
    answer: 'Calculate unit production cost (paper/vinyl sheet cost + laminate + ink + cutter blade wear + packaging sleeve). Add your design/cutting labor time. A standard single sticker retails for $3.50 to $5.00 with wholesale pricing at 50% of retail ($1.75 to $2.50).',
  },
  {
    question: 'How should bulk discounts be structured for stickers?',
    answer: 'Bulk discount tiers typically offer 10% off for 10+ stickers, 20% off for 50+ stickers, and 35% off for 100+ stickers to reward larger order volume while keeping healthy profit margins.',
  },
];

const schemaGraph = [
  {
    '@context': 'https://schema.org',
    '@type': 'WebApplication',
    name: 'Sticker Pricing Calculator',
    url: canonicalUrl,
    description: pageDesc,
    applicationCategory: 'BusinessApplication',
    operatingSystem: 'All',
  },
  {
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: breadcrumbItems.map((item, index) => ({
      '@type': 'ListItem',
      position: index + 1,
      name: item.label,
      item: item.href,
    })),
  },
  {
    '@context': 'https://schema.org',
    '@type': 'FAQPage',
    mainEntity: faqs.map((f) => ({
      '@type': 'Question',
      name: f.question,
      acceptedAnswer: {
        '@type': 'Answer',
        text: f.answer,
      },
    })),
  },
];
---

<SiteLayout title={pageTitle} description={pageDesc} canonical={canonicalUrl}>
  <script is:inline type="application/ld+json" set:html={JSON.stringify(schemaGraph)} />

  <article class="space-y-10 py-2">
    <nav aria-label="Breadcrumb">
      <ol class="flex flex-wrap items-center gap-2 text-xs text-[color:var(--muted)]">
        {breadcrumbItems.map((item, index) => (
          <li class="flex items-center gap-2">
            {index > 0 && <span aria-hidden="true">/</span>}
            {item.href ? (
              <a class="transition hover:text-[color:var(--fg)]" href={item.href}>
                {item.label}
              </a>
            ) : (
              <span aria-current="page" class="font-semibold text-[color:var(--fg)]">
                {item.label}
              </span>
            )}
          </li>
        ))}
      </ol>
    </nav>

    <header class="space-y-3">
      <div class="inline-flex items-center gap-2 rounded-full border border-[color:var(--border)] bg-[color:var(--surface)] px-3.5 py-1 text-xs font-semibold text-[color:var(--fg)]">
        <span>Artist &amp; Vinyl Crafter Economics</span>
      </div>
      <h1 class="text-3xl font-extrabold tracking-tight text-[color:var(--fg)] sm:text-4xl">
        Sticker Pricing Calculator
      </h1>
      <p class="max-w-3xl text-sm leading-relaxed text-[color:var(--muted)] sm:text-base">
        Calculate unit production costs, fair hourly design wages, retail selling prices, and bulk quantity tiers for in-house or outsourced custom sticker batches.
      </p>
    </header>

    <div
      class="rounded-[2.5rem] border border-[color:var(--border)] bg-[color:var(--surface-strong)] p-6 shadow-[var(--shadow)] sm:p-10"
      data-sticker-app
    >
      <div class="grid gap-8 lg:grid-cols-[1.15fr_0.85fr]">
        <form class="space-y-5" onsubmit="return false;">
          <div class="flex items-center justify-between">
            <h2 class="text-lg font-bold text-[color:var(--fg)]">Production Batch Costs</h2>
            <button type="button" class="cursor-pointer text-xs font-semibold text-[color:var(--muted)] hover:text-[color:var(--fg)]" data-reset-btn>
              ↺ Reset
            </button>
          </div>

          <div class="space-y-1.5">
            <label for="batch-qty" class="block text-xs font-bold uppercase tracking-wider text-[color:var(--muted)]">Batch Production Quantity (Total Stickers)</label>
            <input id="batch-qty" type="number" min="1" step="1" value="100" class="w-full rounded-2xl border border-[color:var(--border)] bg-[color:var(--bg)] px-4 py-3 text-base font-semibold text-[color:var(--fg)] outline-none" data-batch-qty />
          </div>

          <div class="grid grid-cols-2 gap-3">
            <div>
              <label for="mat-cost" class="block text-xs font-medium text-[color:var(--muted)]">Total Materials Cost ($)</label>
              <input id="mat-cost" type="number" min="0" step="0.5" value="32.00" class="mt-1 w-full rounded-xl border border-[color:var(--border)] bg-[color:var(--bg)] px-3 py-2 text-xs font-semibold text-[color:var(--fg)] outline-none" data-mat-cost />
              <p class="text-[10px] text-[color:var(--muted)] mt-0.5">Vinyl, laminate, ink, cutter</p>
            </div>
            <div>
              <label for="pack-sleeve" class="block text-xs font-medium text-[color:var(--muted)]">Packaging / Cello Bags ($)</label>
              <input id="pack-sleeve" type="number" min="0" step="0.5" value="8.00" class="mt-1 w-full rounded-xl border border-[color:var(--border)] bg-[color:var(--bg)] px-3 py-2 text-xs font-semibold text-[color:var(--fg)] outline-none" data-pack-cost />
            </div>
          </div>

          <div class="grid grid-cols-2 gap-3 pt-1">
            <div>
              <label for="labor-hours" class="block text-xs font-medium text-[color:var(--muted)]">Production Labor (Hours)</label>
              <input id="labor-hours" type="number" min="0" step="0.25" value="1.5" class="mt-1 w-full rounded-xl border border-[color:var(--border)] bg-[color:var(--bg)] px-3 py-2 text-xs font-semibold text-[color:var(--fg)] outline-none" data-labor-hrs />
            </div>
            <div>
              <label for="hourly-wage" class="block text-xs font-medium text-[color:var(--muted)]">Your Hourly Wage ($/hr)</label>
              <input id="hourly-wage" type="number" min="0" step="1" value="20" class="mt-1 w-full rounded-xl border border-[color:var(--border)] bg-[color:var(--bg)] px-3 py-2 text-xs font-semibold text-[color:var(--fg)] outline-none" data-hourly-wage />
            </div>
          </div>
        </form>

        <div class="flex flex-col justify-between space-y-6 rounded-3xl border border-[color:var(--border)] bg-[color:var(--bg)] p-6 sm:p-8">
          <div class="space-y-5">
            <span class="text-xs font-bold uppercase tracking-wider text-[color:var(--muted)]">Recommended Pricing</span>

            <div class="rounded-2xl border border-[color:var(--border)] bg-[color:var(--surface)] p-6 text-center">
              <p class="text-xs font-semibold text-[color:var(--muted)]">Suggested Retail Price (MSRP)</p>
              <p class="mt-1 text-5xl font-black tracking-tight text-green-600 dark:text-green-400" data-res-retail>
                $3.99
              </p>
              <p class="mt-2 text-xs font-medium text-[color:var(--muted)]" data-res-wholesale>
                Wholesale (50%): $2.00 / sticker
              </p>
            </div>

            <div class="space-y-2 rounded-2xl border border-[color:var(--border)] bg-[color:var(--surface)] p-4 text-xs">
              <div class="flex justify-between">
                <span class="text-[color:var(--muted)]">Unit Production Cost:</span>
                <span class="font-bold text-[color:var(--fg)]" data-unit-cost>$0.70 / sticker</span>
              </div>
              <div class="flex justify-between">
                <span class="text-[color:var(--muted)]">Retail Profit Margin:</span>
                <span class="font-bold text-green-600" data-retail-margin>82.5% ($3.29 profit)</span>
              </div>
              <div class="flex justify-between border-t border-[color:var(--border)] pt-2">
                <span class="text-[color:var(--muted)]">Batch Net Profit (at MSRP):</span>
                <span class="font-bold text-[color:var(--accent)]" data-batch-profit>$329.00</span>
              </div>
            </div>
          </div>

          <!-- Tiered Pricing Matrix -->
          <div class="space-y-2 pt-2 border-t border-[color:var(--border)]">
            <p class="text-xs font-bold uppercase tracking-wider text-[color:var(--muted)]">Bulk Tier Recommendations</p>
            <div class="grid grid-cols-3 gap-2 text-center text-xs">
              <div class="p-2 rounded-xl bg-[color:var(--surface)] border border-[color:var(--border)]">
                <p class="text-[10px] text-[color:var(--muted)]">10+ Pack (-10%)</p>
                <p class="font-bold text-[color:var(--fg)] mt-0.5" data-tier-10>$3.59 ea</p>
              </div>
              <div class="p-2 rounded-xl bg-[color:var(--surface)] border border-[color:var(--border)]">
                <p class="text-[10px] text-[color:var(--muted)]">25+ Pack (-20%)</p>
                <p class="font-bold text-[color:var(--fg)] mt-0.5" data-tier-25>$3.19 ea</p>
              </div>
              <div class="p-2 rounded-xl bg-[color:var(--surface)] border border-[color:var(--border)]">
                <p class="text-[10px] text-[color:var(--muted)]">50+ Bulk (-35%)</p>
                <p class="font-bold text-[color:var(--fg)] mt-0.5" data-tier-50>$2.59 ea</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- FAQs -->
    <section class="space-y-4">
      <h2 class="text-xl font-bold text-[color:var(--fg)]">Frequently Asked Questions</h2>
      <div class="space-y-3">
        {faqs.map((faq) => (
          <details class="group rounded-2xl border border-[color:var(--border)] bg-[color:var(--surface)] p-4 sm:p-5">
            <summary class="cursor-pointer list-none font-semibold text-sm sm:text-base text-[color:var(--fg)] flex items-center justify-between gap-4">
              <span>{faq.question}</span>
              <span class="text-xs text-[color:var(--muted)] group-open:rotate-180 transition-transform">▼</span>
            </summary>
            <p class="mt-3 text-xs sm:text-sm leading-relaxed text-[color:var(--muted)]">
              {faq.answer}
            </p>
          </details>
        ))}
      </div>
    </section>
  </article>

  <script is:inline>
    (function () {
      const root = document.querySelector('[data-sticker-app]');
      if (!root) return;

      const inQty = root.querySelector('[data-batch-qty]');
      const inMat = root.querySelector('[data-mat-cost]');
      const inPack = root.querySelector('[data-pack-cost]');
      const inHrs = root.querySelector('[data-labor-hrs]');
      const inWage = root.querySelector('[data-hourly-wage]');
      const outRetail = root.querySelector('[data-res-retail]');
      const outWholesale = root.querySelector('[data-res-wholesale]');
      const outUnitCost = root.querySelector('[data-unit-cost]');
      const outMargin = root.querySelector('[data-retail-margin]');
      const outBatchProfit = root.querySelector('[data-batch-profit]');
      const outT10 = root.querySelector('[data-tier-10]');
      const outT25 = root.querySelector('[data-tier-25]');
      const outT50 = root.querySelector('[data-tier-50]');
      const resetBtn = root.querySelector('[data-reset-btn]');

      function update() {
        const qty = Math.max(1, parseInt(inQty.value || '1', 10));
        const mat = Math.max(0, parseFloat(inMat.value || '0'));
        const pack = Math.max(0, parseFloat(inPack.value || '0'));
        const hrs = Math.max(0, parseFloat(inHrs.value || '0'));
        const wage = Math.max(0, parseFloat(inWage.value || '0'));

        const totalLabor = hrs * wage;
        const totalBatchCost = mat + pack + totalLabor;
        const unitCost = totalBatchCost / qty;

        // Retail multiplier: minimum $3.50 or 4x unit cost (whichever higher)
        let retail = Math.max(3.50, unitCost * 4);
        retail = Math.ceil(retail * 10) / 10 - 0.01; // Round to .99
        if (retail < unitCost * 2) retail = unitCost * 3.5;

        const wholesale = retail * 0.50;
        const profit = retail - unitCost;
        const margin = retail > 0 ? (profit / retail) * 100 : 0;
        const batchNet = profit * qty;

        if (outRetail) outRetail.textContent = `$${retail.toFixed(2)}`;
        if (outWholesale) outWholesale.textContent = `Wholesale (50%): $${wholesale.toFixed(2)} / sticker`;
        if (outUnitCost) outUnitCost.textContent = `$${unitCost.toFixed(2)} / sticker`;
        if (outMargin) outMargin.textContent = `${margin.toFixed(1)}% ($${profit.toFixed(2)} profit)`;
        if (outBatchProfit) outBatchProfit.textContent = `$${batchNet.toFixed(2)}`;

        if (outT10) outT10.textContent = `$${(retail * 0.90).toFixed(2)} ea`;
        if (outT25) outT25.textContent = `$${(retail * 0.80).toFixed(2)} ea`;
        if (outT50) outT50.textContent = `$${(retail * 0.65).toFixed(2)} ea`;
      }

      inQty.addEventListener('input', update);
      inMat.addEventListener('input', update);
      inPack.addEventListener('input', update);
      inHrs.addEventListener('input', update);
      inWage.addEventListener('input', update);

      if (resetBtn) {
        resetBtn.addEventListener('click', () => {
          inQty.value = '100';
          inMat.value = '32.00';
          inPack.value = '8.00';
          inHrs.value = '1.5';
          inWage.value = '20';
          update();
        });
      }

      update();
    })();
  </script>
</SiteLayout>
