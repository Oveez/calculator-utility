---
import SiteLayout from '../../layouts/SiteLayout.astro';
import Icon from '../../components/Icon.astro';
import { siteName, toAbsoluteUrl, withBasePath } from '../../utils/site';

const pageTitle = `Handmade Product Pricing Calculator | Wholesale (2x) & Retail (4x) · ${siteName}`;
const pageDesc = 'Calculate sustainable craft prices for handmade candles, jewelry, crochet, soap, and woodworking. Computes materials, labor wage, studio overhead, wholesale and MSRP retail.';
const canonicalUrl = toAbsoluteUrl('/pricing-calculators/handmade-product-pricing-calculator/');

const breadcrumbItems = [
  { label: 'Home', href: toAbsoluteUrl('/') },
  { label: 'Pricing Calculators', href: toAbsoluteUrl('/pricing-calculators/') },
  { label: 'Handmade Product Pricing Calculator' },
];

const faqs = [
  {
    question: 'What is the 2x wholesale and 4x retail formula for crafts?',
    answer: 'The maker formula calculates: Base Cost = Materials + Packaging + (Labor Hours × Wage) + Overhead. Wholesale Price = Base Cost × 2 (yielding a 50% gross margin for the maker). Retail MSRP = Wholesale × 2 = Base Cost × 4 (allowing boutique retailers to make their required 50% keystone markup).',
  },
  {
    question: 'Why do so many handmade crafters underprice their work?',
    answer: 'Makers often omit paying themselves a real hourly wage, ignore studio overhead costs (electricity, tools wear, studio rent), or price only for direct retail without leaving room for wholesale retailer margins or craft fair booth fees.',
  },
];

const schemaGraph = [
  {
    '@context': 'https://schema.org',
    '@type': 'WebApplication',
    name: 'Handmade Product Pricing Calculator',
    url: canonicalUrl,
    description: pageDesc,
    applicationCategory: 'BusinessApplication',
    operatingSystem: 'All',
  },
  {
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: breadcrumbItems.map((item, index) => ({
      '@type': 'ListItem',
      position: index + 1,
      name: item.label,
      item: item.href,
    })),
  },
  {
    '@context': 'https://schema.org',
    '@type': 'FAQPage',
    mainEntity: faqs.map((f) => ({
      '@type': 'Question',
      name: f.question,
      acceptedAnswer: {
        '@type': 'Answer',
        text: f.answer,
      },
    })),
  },
];
---

<SiteLayout title={pageTitle} description={pageDesc} canonical={canonicalUrl}>
  <script is:inline type="application/ld+json" set:html={JSON.stringify(schemaGraph)} />

  <article class="space-y-10 py-2">
    <nav aria-label="Breadcrumb">
      <ol class="flex flex-wrap items-center gap-2 text-xs text-[color:var(--muted)]">
        {breadcrumbItems.map((item, index) => (
          <li class="flex items-center gap-2">
            {index > 0 && <span aria-hidden="true">/</span>}
            {item.href ? (
              <a class="transition hover:text-[color:var(--fg)]" href={item.href}>
                {item.label}
              </a>
            ) : (
              <span aria-current="page" class="font-semibold text-[color:var(--fg)]">
                {item.label}
              </span>
            )}
          </li>
        ))}
      </ol>
    </nav>

    <header class="space-y-3">
      <div class="inline-flex items-center gap-2 rounded-full border border-[color:var(--border)] bg-[color:var(--surface)] px-3.5 py-1 text-xs font-semibold text-[color:var(--fg)]">
        <span>Artisan &amp; Maker Craft Economics</span>
      </div>
      <h1 class="text-3xl font-extrabold tracking-tight text-[color:var(--fg)] sm:text-4xl">
        Handmade Product Pricing Calculator
      </h1>
      <p class="max-w-3xl text-sm leading-relaxed text-[color:var(--muted)] sm:text-base">
        Price your handmade candles, jewelry, crochet, soap, and artisan crafts for real profitability. Never work for pennies again.
      </p>
    </header>

    <div
      class="rounded-[2.5rem] border border-[color:var(--border)] bg-[color:var(--surface-strong)] p-6 shadow-[var(--shadow)] sm:p-10"
      data-craft-app
    >
      <div class="grid gap-8 lg:grid-cols-[1.15fr_0.85fr]">
        <form class="space-y-5" onsubmit="return false;">
          <div class="flex items-center justify-between">
            <h2 class="text-lg font-bold text-[color:var(--fg)]">Item Production Costs</h2>
            <button type="button" class="cursor-pointer text-xs font-semibold text-[color:var(--muted)] hover:text-[color:var(--fg)]" data-reset-btn>
              ↺ Reset
            </button>
          </div>

          <div class="grid grid-cols-2 gap-3">
            <div>
              <label for="craft-mat" class="block text-xs font-bold uppercase tracking-wider text-[color:var(--muted)]">Raw Materials ($)</label>
              <input id="craft-mat" type="number" min="0" step="0.5" value="14.50" class="mt-1 w-full rounded-2xl border border-[color:var(--border)] bg-[color:var(--bg)] px-4 py-3 text-base font-semibold text-[color:var(--fg)] outline-none" data-craft-mat />
            </div>
            <div>
              <label for="craft-pack" class="block text-xs font-bold uppercase tracking-wider text-[color:var(--muted)]">Packaging &amp; Tags ($)</label>
              <input id="craft-pack" type="number" min="0" step="0.5" value="2.50" class="mt-1 w-full rounded-2xl border border-[color:var(--border)] bg-[color:var(--bg)] px-4 py-3 text-base font-semibold text-[color:var(--fg)] outline-none" data-craft-pack />
            </div>
          </div>

          <div class="grid grid-cols-2 gap-3">
            <div>
              <label for="craft-hrs" class="block text-xs font-medium text-[color:var(--muted)]">Labor Time (Hours)</label>
              <input id="craft-hrs" type="number" min="0" step="0.25" value="1.25" class="mt-1 w-full rounded-xl border border-[color:var(--border)] bg-[color:var(--bg)] px-3 py-2 text-xs font-semibold text-[color:var(--fg)] outline-none" data-craft-hrs />
            </div>
            <div>
              <label for="craft-wage" class="block text-xs font-medium text-[color:var(--muted)]">Your Hourly Wage ($/hr)</label>
              <input id="craft-wage" type="number" min="0" step="1" value="22" class="mt-1 w-full rounded-xl border border-[color:var(--border)] bg-[color:var(--bg)] px-3 py-2 text-xs font-semibold text-[color:var(--fg)] outline-none" data-craft-wage />
            </div>
          </div>

          <div class="space-y-1.5 pt-1">
            <label for="craft-overhead" class="block text-xs font-medium text-[color:var(--muted)]">Studio Overhead &amp; Tool Wear (% of Base Cost)</label>
            <input id="craft-overhead" type="number" min="0" max="50" step="1" value="15" class="w-full rounded-xl border border-[color:var(--border)] bg-[color:var(--bg)] px-3 py-2 text-xs font-semibold text-[color:var(--fg)] outline-none" data-craft-overhead />
            <p class="text-[10px] text-[color:var(--muted)]">Covers electricity, equipment amortization, marketing &amp; listing fees.</p>
          </div>
        </form>

        <div class="flex flex-col justify-between space-y-6 rounded-3xl border border-[color:var(--border)] bg-[color:var(--bg)] p-6 sm:p-8">
          <div class="space-y-5">
            <span class="text-xs font-bold uppercase tracking-wider text-[color:var(--muted)]">Pricing Framework</span>

            <div class="rounded-2xl border border-[color:var(--border)] bg-[color:var(--surface)] p-6 text-center">
              <p class="text-xs font-semibold text-[color:var(--muted)]">Suggested Retail Price (MSRP)</p>
              <p class="mt-1 text-5xl font-black tracking-tight text-green-600 dark:text-green-400" data-res-retail>
                $204.00
              </p>
              <p class="mt-2 text-xs font-medium text-[color:var(--muted)]" data-res-wholesale>
                Wholesale Price (2x): $102.00
              </p>
            </div>

            <div class="space-y-2 rounded-2xl border border-[color:var(--border)] bg-[color:var(--surface)] p-4 text-xs">
              <div class="flex justify-between">
                <span class="text-[color:var(--muted)]">Total Item Cost (Materials + Labor + Overhead):</span>
                <span class="font-bold text-[color:var(--fg)]" data-base-cost>$51.18</span>
              </div>
              <div class="flex justify-between">
                <span class="text-[color:var(--muted)]">Direct Retail Profit:</span>
                <span class="font-bold text-green-600" data-direct-profit>$152.82 (74.9% margin)</span>
              </div>
              <div class="flex justify-between border-t border-[color:var(--border)] pt-2">
                <span class="text-[color:var(--muted)]">Wholesale Profit:</span>
                <span class="font-bold text-[color:var(--fg)]" data-wholesale-profit>$50.82 (50.0% margin)</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- FAQs -->
    <section class="space-y-4">
      <h2 class="text-xl font-bold text-[color:var(--fg)]">Frequently Asked Questions</h2>
      <div class="space-y-3">
        {faqs.map((faq) => (
          <details class="group rounded-2xl border border-[color:var(--border)] bg-[color:var(--surface)] p-4 sm:p-5">
            <summary class="cursor-pointer list-none font-semibold text-sm sm:text-base text-[color:var(--fg)] flex items-center justify-between gap-4">
              <span>{faq.question}</span>
              <span class="text-xs text-[color:var(--muted)] group-open:rotate-180 transition-transform">▼</span>
            </summary>
            <p class="mt-3 text-xs sm:text-sm leading-relaxed text-[color:var(--muted)]">
              {faq.answer}
            </p>
          </details>
        ))}
      </div>
    </section>
  </article>

  <script is:inline>
    (function () {
      const root = document.querySelector('[data-craft-app]');
      if (!root) return;

      const inMat = root.querySelector('[data-craft-mat]');
      const inPack = root.querySelector('[data-craft-pack]');
      const inHrs = root.querySelector('[data-craft-hrs]');
      const inWage = root.querySelector('[data-craft-wage]');
      const inOverhead = root.querySelector('[data-craft-overhead]');
      const outRetail = root.querySelector('[data-res-retail]');
      const outWholesale = root.querySelector('[data-res-wholesale]');
      const outBase = root.querySelector('[data-base-cost]');
      const outDirect = root.querySelector('[data-direct-profit]');
      const outWholesaleProfit = root.querySelector('[data-wholesale-profit]');
      const resetBtn = root.querySelector('[data-reset-btn]');

      function update() {
        const mat = Math.max(0, parseFloat(inMat.value || '0'));
        const pack = Math.max(0, parseFloat(inPack.value || '0'));
        const hrs = Math.max(0, parseFloat(inHrs.value || '0'));
        const wage = Math.max(0, parseFloat(inWage.value || '0'));
        const overheadPct = Math.max(0, parseFloat(inOverhead.value || '0')) / 100;

        const directLabor = hrs * wage;
        const directCost = mat + pack + directLabor;
        const totalBaseCost = directCost * (1 + overheadPct);

        const wholesale = totalBaseCost * 2;
        const retail = wholesale * 2;

        const directProfit = retail - totalBaseCost;
        const wholesaleProfit = wholesale - totalBaseCost;

        if (outRetail) outRetail.textContent = `$${retail.toFixed(2)}`;
        if (outWholesale) outWholesale.textContent = `Wholesale Price (2x): $${wholesale.toFixed(2)}`;
        if (outBase) outBase.textContent = `$${totalBaseCost.toFixed(2)}`;
        if (outDirect) outDirect.textContent = `$${directProfit.toFixed(2)} (${((directProfit / retail) * 100).toFixed(1)}% margin)`;
        if (outWholesaleProfit) outWholesaleProfit.textContent = `$${wholesaleProfit.toFixed(2)} (50.0% margin)`;
      }

      inMat.addEventListener('input', update);
      inPack.addEventListener('input', update);
      inHrs.addEventListener('input', update);
      inWage.addEventListener('input', update);
      inOverhead.addEventListener('input', update);

      if (resetBtn) {
        resetBtn.addEventListener('click', () => {
          inMat.value = '14.50';
          inPack.value = '2.50';
          inHrs.value = '1.25';
          inWage.value = '22';
          inOverhead.value = '15';
          update();
        });
      }

      update();
    })();
  </script>
</SiteLayout>
