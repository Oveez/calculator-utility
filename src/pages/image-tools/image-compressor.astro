---
import SiteLayout from '../../layouts/SiteLayout.astro';
import Icon from '../../components/Icon.astro';
import { siteName, toAbsoluteUrl, withBasePath } from '../../utils/site';

const pageTitle = `Client-Side Image Compressor — Compress JPEG, WebP, PNG Online · ${siteName}`;
const pageDesc = 'Compress JPEG, WebP, and PNG images locally in your browser. Adjustable quality slider, live file weight comparison, and instant download with zero server uploads.';
const canonicalUrl = toAbsoluteUrl('/image-tools/image-compressor/');

const breadcrumbItems = [
  { label: 'Home', href: toAbsoluteUrl('/') },
  { label: 'Image Tools', href: toAbsoluteUrl('/image-tools/') },
  { label: 'Image Compressor' },
];

const schemaFaq = [
  {
    question: 'How does client-side image compression work?',
    answer: 'The browser renders your image into an HTML5 Canvas and uses `canvas.toBlob(callback, type, quality)` with your chosen compression quality factor, reducing file size in memory.',
  },
];

const schemaGraph = [
  {
    '@context': 'https://schema.org',
    '@type': 'WebApplication',
    name: 'Client-Side Image Compressor',
    url: canonicalUrl,
    applicationCategory: 'ImageApplication',
    operatingSystem: 'All',
    browserRequirements: 'Requires JavaScript',
    description: pageDesc,
  },
  {
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: breadcrumbItems.map((item, index) => ({
      '@type': 'ListItem',
      position: index + 1,
      name: item.label,
      item: item.href,
    })),
  },
  {
    '@context': 'https://schema.org',
    '@type': 'FAQPage',
    mainEntity: schemaFaq.map((item) => ({
      '@type': 'Question',
      name: item.question,
      acceptedAnswer: {
        '@type': 'Answer',
        text: item.answer,
      },
    })),
  },
];
---

<SiteLayout title={pageTitle} description={pageDesc} canonical={canonicalUrl}>
  <script is:inline type="application/ld+json" set:html={JSON.stringify(schemaGraph)} />

  <article class="space-y-10 py-2" data-compressor-app>
    <nav aria-label="Breadcrumb">
      <ol class="flex flex-wrap items-center gap-2 text-xs text-[color:var(--muted)]">
        {breadcrumbItems.map((item, index) => (
          <li class="flex items-center gap-2">
            {index > 0 && <span aria-hidden="true">/</span>}
            {item.href ? (
              <a class="transition hover:text-[color:var(--fg)]" href={item.href}>
                {item.label}
              </a>
            ) : (
              <span aria-current="page" class="font-semibold text-[color:var(--fg)]">
                {item.label}
              </span>
            )}
          </li>
        ))}
      </ol>
    </nav>

    <header class="space-y-3">
      <div class="inline-flex items-center gap-2 rounded-full border border-[color:var(--border)] bg-[color:var(--surface)] px-3 py-1 text-xs font-semibold text-[color:var(--muted)]">
        <span class="inline-block h-2 w-2 rounded-full bg-emerald-500"></span>
        <span>100% In-Browser Compression</span>
      </div>
      <h1 class="text-2xl sm:text-4xl font-extrabold tracking-tight text-[color:var(--fg)]">
        Client-Side Image Compressor
      </h1>
      <p class="max-w-2xl text-xs sm:text-sm text-[color:var(--muted)] leading-relaxed">
        Reduce image file sizes instantly without sending any photos to external servers.
      </p>
    </header>

    <div class="space-y-6">
      <div id="dropzone-box" class="space-y-3 rounded-3xl border-2 border-dashed border-[color:var(--border)] bg-[color:var(--surface)] p-6 sm:p-8 text-center transition hover:border-[color:var(--border-strong)]">
        <input type="file" id="img-file" accept="image/*" class="hidden" />
        
        <label for="img-file" id="upload-prompt" class="cursor-pointer space-y-2 block">
          <div class="mx-auto flex h-12 w-12 items-center justify-center rounded-2xl bg-[color:var(--surface-strong)] text-[color:var(--fg)]">
            <Icon name="image" class="w-6 h-6 text-[color:var(--accent)]" />
          </div>
          <p class="text-sm font-bold text-[color:var(--fg)]">Choose an image to compress</p>
          <p class="text-xs text-[color:var(--muted)]">Supports JPG, PNG, and WebP (100% private in browser)</p>
        </label>

        <!-- Visual Upload Preview Box -->
        <div id="upload-preview-box" class="hidden flex flex-col sm:flex-row items-center justify-between gap-4 rounded-2xl border border-[color:var(--border)] bg-[color:var(--bg)] p-3.5 text-left">
          <div class="flex items-center gap-3 min-w-0">
            <div class="h-14 w-14 rounded-xl bg-[color:var(--surface-strong)] overflow-hidden shrink-0 flex items-center justify-center border border-[color:var(--border)]">
              <img id="thumb-preview" alt="Thumbnail" class="max-h-full max-w-full object-cover" />
            </div>
            <div class="min-w-0 space-y-0.5">
              <p id="thumb-name" class="text-xs sm:text-sm font-bold text-[color:var(--fg)] truncate">image.jpg</p>
              <p id="thumb-info" class="text-[11px] text-[color:var(--muted)]">0 KB</p>
            </div>
          </div>
          <label for="img-file" class="cursor-pointer shrink-0 rounded-xl border border-[color:var(--border)] bg-[color:var(--surface)] px-3 py-1.5 text-xs font-semibold text-[color:var(--muted)] hover:text-[color:var(--fg)] transition">
            Change Image
          </label>
        </div>
      </div>

      <!-- Controls & Preview -->
      <div id="comp-wrapper" class="hidden space-y-6">
        <div class="grid gap-4 sm:grid-cols-2 rounded-2xl border border-[color:var(--border)] bg-[color:var(--surface)] p-5 text-xs">
          <div>
            <div class="flex justify-between mb-1.5 font-bold text-[color:var(--fg)]">
              <span>Compression Quality:</span>
              <span id="qual-val" class="font-mono text-emerald-500">80%</span>
            </div>
            <input
              type="range"
              id="qual-slider"
              min="10"
              max="100"
              value="80"
              class="w-full accent-[color:var(--fg)] cursor-pointer"
            />
          </div>
          <div>
            <label for="out-format" class="font-bold text-[color:var(--fg)] block mb-1.5">Output Format:</label>
            <select id="out-format" class="w-full rounded-xl border border-[color:var(--border)] bg-[color:var(--bg)] px-3 py-1.5 text-[color:var(--fg)]">
              <option value="image/jpeg">JPEG (Standard)</option>
              <option value="image/webp">WebP (High Efficiency)</option>
            </select>
          </div>
        </div>

        <div class="grid gap-4 sm:grid-cols-3 text-center">
          <div class="rounded-2xl border border-[color:var(--border)] bg-[color:var(--surface)] p-4">
            <span class="text-xs text-[color:var(--muted)]">Original Size</span>
            <p id="orig-size" class="text-xl font-bold text-[color:var(--fg)]">-</p>
          </div>
          <div class="rounded-2xl border border-[color:var(--border)] bg-[color:var(--surface)] p-4">
            <span class="text-xs text-[color:var(--muted)]">Compressed Size</span>
            <p id="comp-size" class="text-xl font-bold text-emerald-500">-</p>
          </div>
          <div class="rounded-2xl border border-[color:var(--border)] bg-[color:var(--surface)] p-4">
            <span class="text-xs text-[color:var(--muted)]">Savings</span>
            <p id="savings-val" class="text-xl font-bold text-emerald-500">-</p>
          </div>
        </div>

        <button
          type="button"
          id="dl-comp-btn"
          class="w-full rounded-xl bg-[color:var(--fg)] py-3 text-xs font-bold text-[color:var(--bg)] shadow-xs transition hover:opacity-90"
        >
          Download Compressed Image
        </button>
      </div>
    </div>

    <!-- FAQs -->
    <section class="space-y-4">
      <h2 class="text-xl font-bold text-[color:var(--fg)]">Frequently Asked Questions</h2>
      <div class="space-y-3">
        {schemaFaq.map((faq) => (
          <details class="group rounded-2xl border border-[color:var(--border)] bg-[color:var(--surface)] p-4 sm:p-5">
            <summary class="cursor-pointer list-none font-semibold text-sm sm:text-base text-[color:var(--fg)] flex items-center justify-between gap-4">
              <span>{faq.question}</span>
              <span class="text-xs text-[color:var(--muted)] group-open:rotate-180 transition-transform">▼</span>
            </summary>
            <p class="mt-3 text-xs sm:text-sm leading-relaxed text-[color:var(--muted)]">
              {faq.answer}
            </p>
          </details>
        ))}
      </div>
    </section>
  </article>

  <script is:inline>
    (function () {
      const root = document.querySelector('[data-compressor-app]');
      if (!root) return;

      const fileIn = root.querySelector('#img-file');
      const uploadPrompt = root.querySelector('#upload-prompt');
      const uploadPreviewBox = root.querySelector('#upload-preview-box');
      const thumbPreview = root.querySelector('#thumb-preview');
      const thumbName = root.querySelector('#thumb-name');
      const thumbInfo = root.querySelector('#thumb-info');
      const wrapper = root.querySelector('#comp-wrapper');
      const qualSlider = root.querySelector('#qual-slider');
      const qualVal = root.querySelector('#qual-val');
      const outFmt = root.querySelector('#out-format');
      const origSizeEl = root.querySelector('#orig-size');
      const compSizeEl = root.querySelector('#comp-size');
      const savingsEl = root.querySelector('#savings-val');
      const dlBtn = root.querySelector('#dl-comp-btn');

      let loadedImg = null;
      let origSizeBytes = 0;
      let compBlobUrl = null;
      let origFileName = 'image.jpg';

      function formatBytes(bytes) {
        if (bytes < 1024) return bytes + ' B';
        if (bytes < 1048576) return (bytes / 1024).toFixed(1) + ' KB';
        return (bytes / 1048576).toFixed(2) + ' MB';
      }

      function compress() {
        if (!loadedImg) return;
        const quality = parseInt(qualSlider.value, 10) / 100;
        qualVal.textContent = `${qualSlider.value}%`;
        const fmt = outFmt.value;

        const canvas = document.createElement('canvas');
        canvas.width = loadedImg.naturalWidth;
        canvas.height = loadedImg.naturalHeight;
        const ctx = canvas.getContext('2d');
        ctx.drawImage(loadedImg, 0, 0);

        canvas.toBlob((blob) => {
          if (compBlobUrl) URL.revokeObjectURL(compBlobUrl);
          compBlobUrl = URL.createObjectURL(blob);
          compSizeEl.textContent = formatBytes(blob.size);

          const saved = Math.max(0, origSizeBytes - blob.size);
          const pct = ((saved / origSizeBytes) * 100).toFixed(0);
          savingsEl.textContent = `${pct}% (${formatBytes(saved)})`;
        }, fmt, quality);
      }

      fileIn.addEventListener('change', (e) => {
        const file = e.target.files[0];
        if (!file) return;

        origFileName = file.name;
        origSizeBytes = file.size;
        origSizeEl.textContent = formatBytes(file.size);

        const url = URL.createObjectURL(file);
        thumbPreview.src = url;
        thumbName.textContent = file.name;
        thumbInfo.textContent = `${formatBytes(file.size)} • ${file.type || 'image'}`;

        uploadPrompt.classList.add('hidden');
        uploadPreviewBox.classList.remove('hidden');

        const img = new Image();
        img.onload = () => {
          loadedImg = img;
          thumbInfo.textContent = `${img.naturalWidth}×${img.naturalHeight} px • ${formatBytes(file.size)}`;
          wrapper.classList.remove('hidden');
          compress();
        };
        img.src = url;
      });

      qualSlider.addEventListener('input', compress);
      outFmt.addEventListener('change', compress);

      dlBtn.addEventListener('click', () => {
        if (!compBlobUrl) return;
        const a = document.createElement('a');
        a.href = compBlobUrl;
        const ext = outFmt.value === 'image/webp' ? '.webp' : '.jpg';
        const base = origFileName.substring(0, origFileName.lastIndexOf('.')) || origFileName;
        a.download = `${base}_compressed${ext}`;
        a.click();
      });
    })();
  </script>
</SiteLayout>
